{
  "name": "maxfield-medical",
  "version": "1.0.0",
  "description": "1. `git clone https://github.com/samuraijane/maxfield-medical.git` 1. `cd maxfield-medical` 1. `npm i` 1. `npm run build` 1. `npm run start` 1. Navigate to localhost:3000",
  "main": "server.js",
  "scripts": {
    "build": "cd react-ui/ && npm install && npm run build",
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
